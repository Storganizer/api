<search>


  <scan-qr-code></scan-qr-code>


  <div class="search"><input onkeyup={typeahead} type="text" placeholder="🔍 search" id="search" name="search"></div>

  <div class="results">
    <locations-list if={ state.locations.length } locations={ state.locations }></locations-list>
    <boxes-list if={ state.boxes.length }  boxes={ state.boxes }></boxes-list>
    <items-list if={ state.items.length }  items={ state.items }></items-list>
  </div>

  <script>
    import  './style.css'

    import ScanQrCode from '../../includes/helper/scan-qr-code.riot'
    import LocationsList from '../../includes/location/list.riot'
    import BoxesList from '../../includes/list/boxes.riot'
    import ItemsList from '../../includes/list/items.riot'

    export default {
      components: {
        ScanQrCode,
        LocationsList,
        BoxesList,
        ItemsList,
      },

      state: {
        locations: [],
        boxes: [],
        items: [],
      },

      typeahead(e, allowEmpty = false) {
        let searchString = e.target.value
        this.search(searchString)
      },

      search(searchString, allowEmpty = false) {
        let target = this
        if (searchString.length >= 3 || allowEmpty) {
          this.registry.dataStore.locations.search(searchString, function(locations) {
            target.update({
              locations: locations
            })
          })

          this.registry.dataStore.boxes.search(searchString, function(boxes) {
            target.update({
              boxes: boxes
            })
          })

          this.registry.dataStore.items.search(searchString, function(items) {
            target.update({
              items: items
            })
          })
        }
      },

      onMounted() {
        this.search('', true)
      }

    }
  </script>
</search>